server:
  port: 8086
spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:lab_db.db
    username: user
    password: pass
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true

